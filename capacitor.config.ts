import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.meteo.ics',
  appName: 'My Weather',
  webDir: 'dist/meteo-front/browser'
};

export default config;
